{
  "name": "terminal-mobile-server",
  "version": "1.0.0",
  "description": "Remote intelligent terminal server for TerminalMobile Android app",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "token": "node -e \"const c=require('crypto');console.log(c.randomBytes(32).toString('hex'))\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-pty": "^1.1.0",
    "uuid": "^9.0.1",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
